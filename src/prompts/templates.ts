export enum LLM_TEMPLATE {
  TAB_DESCRIPTION = "You are to suggest a 3-5 word short description which semantically links the following list of keywords",
  GROUP_LABEL = "You are to suggest a 1-3 word label for the following list of descriptions",
}

export enum TOKEN_SEPARATOR {
  SEMI_COLON = ":",
  COMMA = ",",
  SPACE = " ",
  PERIOD = ".",
}
